# Crumbless CLI

CLI for managing Crumbless social media AI autopilot brands from the terminal.

## Installation

```bash
curl -sSL https://raw.githubusercontent.com/crumblessai/crumbless-cli/main/scripts/install.sh | bash
```

## Commands

### Overview
- `crumbless brands` — List all brands
- `crumbless dashboard <slug>` — Full brand dashboard
- `crumbless status <slug>` — Detailed status

### Posts
- `crumbless content <slug> [--status <status>]` — List/filter posts (status: pending_user, approved, scheduled, published, failed)
- `crumbless approve <slug> [--all] [--dry]` — Approve pending posts
- `crumbless post <slug> <id>` — Show post: text, media, carousel slides
- `crumbless post <slug> <id> edit [--caption|--title|--link|--subreddit|--firstComment|--imagePrompt|--media|--format|--platforms|--platformCaption|--slot|--product]` — Edit fields (no render, no credits)
- `crumbless post <slug> <id> regenerate --instruction "warmer background"` — Refine a single image (bills one render)
- `crumbless post <slug> <id> slide --index 1 --instruction "..."` — Re-render ONE carousel slide (0 = cover)
- `crumbless post <slug> <id> reorder --order "0,2,1"` — Reorder/drop slides (no render)
- `crumbless post <slug> <id> video [--duration 6] [--script "..."] [--instruction "..."]` — Animate the cover into a clip; also retries a video that fell back to a photo
- `crumbless post <slug> <id> render` — Generate the missing image from the prompt
- `crumbless post <slug> <id> approve` — Approve single post
- `crumbless post <slug> <id> publish` — Publish immediately
- `crumbless post <slug> <id> reschedule --scheduledFor "2026-06-20T10:00"` — Reschedule
- `crumbless post <slug> <id> reject` — Delete pending post

Editing a SCHEDULED post re-syncs it to Zernio automatically, so what goes out is what you edited.

### Editorial Plan
- `crumbless plan <slug>` — View active plan + proposals
- `crumbless plan <slug> propose` — Generate first editorial plan
- `crumbless plan <slug> revise --feedback "..."` — Request revision
- `crumbless plan <slug> approve` — Approve proposed plan
- `crumbless plan <slug> discard` — Discard proposed plan
- `crumbless plan <slug> save-brief --week 0 --brief "..."` — Save week brief
- `crumbless plan <slug> replan --week 0 --brief "..."` — Regenerate week

### Weekly Plan
- `crumbless weekly-plan <slug> [--week N]` — View weekly plan + seeds
- `crumbless weekly-plan <slug> plan --week N` — Generate seeds for a week
- `crumbless weekly-plan <slug> produce --week N` — Produce seeds into posts

### Strategy
- `crumbless gtm <slug>` — View GTM roadmap
- `crumbless voice <slug>` — View voice rules

### Studio
- `crumbless studio <slug>` — View full studio
- `crumbless studio <slug> kit-update --about "..." [--category "..."] [--audience "..."]` — Update brand kit
- `crumbless studio <slug> colors --colors "#hex,#hex"` — Set brand colors
- `crumbless studio <slug> add-note --title "..." --text "..."` — Add knowledge note
- `crumbless studio <slug> delete-doc --id <uuid>` — Delete document
- `crumbless studio <slug> people-add --name "..." --consent [--role "..."]` — Add real person (`--consent` attests you have their consent; without it the API refuses)
- `crumbless studio <slug> people-generate --name "..." [--gender female] [--vibe professional]` — Generate AI person
- `crumbless studio <slug> people-delete --id <uuid>` — Delete person
- `crumbless studio <slug> add-competitor --name "..." [--website "..."]` — Add competitor
- `crumbless studio <slug> delete-competitor --id <uuid>` — Delete competitor
- `crumbless studio <slug> research` — AI competitor research
- `crumbless studio <slug> sync-history` — Sync social post history

### SEO
- `crumbless seo <slug>` — Tech score, search performance, SEO grade + initiatives
- `crumbless seo <slug> run` — Run the technical audit (also refreshes the plan)
- `crumbless seo <slug> plan` — Generate the SEO growth plan
- `crumbless seo <slug> more [--guidance "..."]` — Append more initiatives
- `crumbless seo <slug> asset --id <initiativeId>` — Turn an initiative into a ready asset
- `crumbless seo <slug> article --id <initiativeId>` — Turn an initiative into a full article draft

### GEO (AI visibility)
- `crumbless geo <slug>` — Share of voice, per-prompt citations, ready fixes
- `crumbless geo <slug> run` — Run the citation audit
- `crumbless geo <slug> fix` — Generate fix artifacts for the gaps found

### Keywords
- `crumbless keywords <slug>` — Keyword strategy: volume, difficulty, opportunity, action
- `crumbless keywords <slug> refresh` — Regenerate the keyword research

### Web / Blog
- `crumbless web <slug> [--status draft|scheduled|published|all]` — List articles (drafts included)
- `crumbless web <slug> generate --topic "..."` — Generate an article draft from a topic
- `crumbless web <slug> optimize --id <id>` — Rewrite for SEO score (meta title/description included)
- `crumbless web <slug> publish|unpublish|delete --id <id>` — Manage an article

Note: `--id` accepts the short prefix printed in the tables.

### Ads
- `crumbless ads <slug>` — Ad campaigns, spend, metrics + boost candidates
- `crumbless ads <slug> --sync` — Sync ad accounts + metrics
- `crumbless ads <slug> --propose` — Propose boosts from top organic posts
- `crumbless ads <slug> --create --name "..." --headline "..." [--platform metaads|googleads] [--budget N]` — Create a standalone proposal
- `crumbless ads <slug> --approve <id> [--budget N] [--goal ...]` — Approve + launch on Zernio
- `crumbless ads <slug> --pause <id>` — Pause a campaign (all its creatives)
- `crumbless ads <slug> --resume <id>` — Resume a paused campaign
- `crumbless ads <slug> --pause <id> --ad <adId>` — Pause one creative only (A/B)
- `crumbless ads <slug> --resume <id> --ad <adId>` — Resume one creative only
- `crumbless ads <slug> --duplicate <id>` — Duplicate a campaign (paused copy as new proposal)
- `crumbless ads <slug> --delete <id>` — Delete a campaign on the platform (history kept)
- `crumbless ads <slug> --reject <id>` — Reject a proposal

### Analytics
- `crumbless analytics <slug>` — View analytics
- `crumbless calendar <slug> [--month YYYY-MM]` — View calendar

### AI Chat
- `crumbless ai <slug> --message "..."` — Chat with AI (full read/write access)
- `echo "..." | crumbless ai <slug>` — Pipe mode for AI agents
- `crumbless ai <slug> --message "..." --pipe` — Raw output (no formatting)

### System
- `crumbless update` — Update CLI to latest version
- `crumbless upgrade <slug>` — Open plan upgrade page
- `021 --help` — Show help

## Environment

- `PUBLIC_APP_URL` — Backend URL (default: http://localhost:5173)

## Auth

Run `crumbless login` once to authenticate via browser, or non-interactively with `crumbless login --email you@example.com --password …` (or `--password-stdin` to keep the password out of shell history and `ps`). Session is saved in `~/.config/crumbless/session.json`.

## MCP

MCP server in `mcp/` — stdio + Streamable HTTP. Same OAuth identity as the CLI (no static API tokens).

```bash
bun run mcp          # stdio
bun run mcp:http     # http://localhost:8787/mcp
```

Remote (Vercel / `mcp.crumbless.ai`): `Authorization: Bearer <access_token>`. Full map: `docs/mcp.md`.

## Observability (Sentry + Supabase)

MCP HTTP logs/errors → Vercel stderr, optional Sentry (`SENTRY_DSN`), optional Supabase `mcp_logs` (`SUPABASE_SERVICE_ROLE_KEY`). See `docs/mcp.md`.

## Architecture

The CLI is a thin HTTP client. All operations go through `/api/v1/*` endpoints. No direct database access. No secrets embedded.

```
CLI / MCP → HTTP → /api/v1/* → Supabase → Database
```
